{
  "name": "votechain-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm run dev --workspace=@votechain/frontend",
    "dev:backend": "npm run dev --workspace=@votechain/backend",
    "vercel-build": "npx prisma generate --schema=apps/backend/prisma/schema.prisma && npm run build --workspace=@votechain/common && npm run build --workspace=@votechain/backend && npm run build --workspace=@votechain/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
